import 'package:flutter_driver/flutter_driver.dart';

import 'package:shared/shared.dart';

// detail screen
final tapItemFavoriteKey = find.byValueKey(AppConstants.USERS);
final tapBackButtonKey = find.byValueKey(AppConstants.TEACHERS);

/*
// now playing screen
final tapListViewNowPlayingKey = find.byValueKey(KEY_LIST_VIEW_NOW_PLAYING);
final tapItemNowPlayingKey = find.byValueKey(KEY_TAP_ITEM_NOW_PLAYING);
*/
